import {StatusBar} from 'expo-status-bar';
import {useState} from 'react';
import {Button, Keyboard, StyleSheet, Text, TextInput, View, Alert} from 'react-native';

export default function Buscar(){
    const [cep, setCep] = useState('');
}
